<h1>Register</h1>
<form action="/users/register" method="POST">
    <ul>
        <% if(typeof errors !='undefined') {%> <% errors.forEach(error=>{%>
               <li><%=error.message %></li>u
        <% }) %> <% } %>
    </ul>
    <div>
        <input type="text" id="name" name="name" placeholder="enter name" required>
    </div>
    <div>
        <input type="email" id="email" name="email" placeholder="enter email" required>
    </div>
    <div>
        <input type="password" id="password" name="password" placeholder="enter password" required>
    </div>
    <div>
        <input type="password" id="password2" name="password2" placeholder="confirm password" required>
    </div>
    <div>
        <input type="submit"  value="Register">
    </div>
    <div>
        <a href="/users/login">Already registered?..Login here</a>
    </div>
</form>


<!-- <script>
   var name = [];
    var email=[];
    var password2=[];
    var password = [];
    var i=0;
    nome[i] = document.getElementById('name');
    password[i] = document.getElementById('password');
    email[i] = document.getElementById('email');
    password2[i] = document.getElementById('password2');



    
    function store() {
        localStorage.setItem('name', JSON.stringify(name));
        localStorage.setItem('email', JSON.stringify(email));
        localStorage.setItem('password', JSON.stringify(password));

        localStorage.setItem('password2', JSON.stringify(password2));

        i++;
    }
</script> -->
